# Attendance repair — local execution

Approved outcome: one session-scoped register for Branch and assigned, explicitly granted Coach accounts; preserved canonical roster and finalization effects. No production/provider changes.

Design: session identity and counts above one compact roster; unresolved entries first, per-row status, explicit bulk unmarked-present, undo unsaved changes; shared saved draft and revision conflict. A sticky Finish attendance action commits the saved register. Existing correction/delivery controls remain separate. Photo capture is optional and never creates an absence.

Execution: preserve dirty source; reproduce current Coach/Branch; database regression for complete safe roster, shared revisions, denied/revoked access, retries and existing downstream effects; shared component and entry points; private camera/upload and locally runnable recognition investigation; synthetic-only test set; measured separate-account browser journey and bilingual responsive checks; current build/type/lint and evidence. Recognition may not be represented as complete without genuine distinct-image matching and integrated consent/version/revocation safeguards.

Source of current defects: academy-panel AttendanceEditor derives a staff roster from paginated workspace data, keeps unshared React marks and bulk-overwrites exceptions; CoachToday links to a collapsed assigned-session disclosure labeled Open roster. Coach writing already requires attendance.finalize plus current assignment; preserve that boundary.

Synchronization target: visible authorized data within 2 s from successful database commit on local foreground browser sessions; recognition duration measured separately. Poll interval 1.2 s, scoped by session, while editing conflicts must never replace local work silently.
