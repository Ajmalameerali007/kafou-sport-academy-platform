# KAFOU completion and hosted verification — 20 September 2026

This is a targeted continuation of the operational application. No portal redesign, replacement Site, database reset, audience change, live customer intake or provider activation occurred. The user's latest instruction explicitly keeps Payment Gateway and WhatsApp inactive until vendor selection and approved test configuration.

## Local release preparation follow-up

The user subsequently instructed “u do localy.” Hosted investigation stopped after read-only checks; publishing, schema writes and hosted acceptance are deferred. This follow-up preserves the original local completion and adds a reproducible local release, a refreshed SQL equivalence map and populated upgrade/recovery evidence. The new build identity deliberately differs because the provenance allowlist now includes authentication middleware, build plugins, hooks, vendor styles, binding metadata and other build inputs previously omitted. Local secrets remain excluded, with a regression test.

All 501 initial tracked/nonignored source and document files were preserved in a private local source checkpoint. No existing changes were staged, committed, reset or discarded, and no remote was substituted. The authenticated `/api/release` matches the new local artifact; anonymous access returns 401. Source, artifact and evidence hashes are linked in `evidence/release-preparation.json`. The updated `evidence/completion-summary.json` is the current summary; the initial summary remains in the preserved prior evidence archive.

The mapped upgrade reconstruction applies the 32 SQL-equivalent historical migrations in actual hosted order, loads declared synthetic family/coach/session/enrollment/offline-payment fixtures, backs up and restores that populated database into a second isolated database, and then applies exactly the 10 missing migrations. It checks original-column row fingerprints, commercial totals, current-parent isolation and normal AAL1 owner denial. Both disposable databases are dropped. This establishes a populated reconstructed-schema upgrade and database recovery, not a hosted data restore, Auth-service recovery, file restoration or prior-Worker traffic rollback.

Separately, a fresh local full database restore compared 158 tables and 1,623 rows with exact fingerprints and sequence positions; the source was unchanged and the private dump was deleted. Provider/Auth configuration, globals and file bytes remain outside that rehearsal.

## Source and environments

- Operational checkout: `/Users/viova/Documents/ChatGPT/SPORTS K`, branch `codex/kafou-public`, base commit `ac9ef030c660fb08a1c5789cfa9c32f27f278492`. Existing and new operational changes are uncommitted. The base commit alone does not identify this build.
- `lib/platform/build-provenance.json` is generated from allowlisted source inputs at build time; `/api/release` exposes the source fingerprint and expected migration hashes to authenticated sessions. Evidence records actual artifact hashes separately. No secret configuration is fingerprinted or returned.
- Local compiled preview: `http://127.0.0.1:3101`; isolated Auth/database: `http://127.0.0.1:56321`, container `supabase_db_kafou-local`. New migrations were applied additively here after clean replay.
- Preserved private Site: `appgprj_6aaeb83ca7cc8191a4db59b84670602c`, `https://kafou-sport-academy.ajazx800.chatgpt.site/`. Connector get/search still reports 404/no match. Browser ChatGPT sign-in opens the actual public-facing academy page and application authentication screen. The new `/register` route returns the academy404. Thus it serves an earlier build, not this completion build. Current deployed version/source cannot be certified without restored connector access. The latest user direction defers that work and keeps execution local.
- Supabase staging: `cwdazidovxqeevmpicng`. Read-only native CLI queries found 32 remote migrations. All 32 stored statements exactly match local SQL after joining stored statements and trimming file-boundary whitespace, although product migrations use different version IDs. The full SHA256/version/name map is in `evidence/hosted-migration-map.json`. Do **not** run a blind `db push`, duplicate those migrations, or repair history based only on filenames.
- Hosted read-only activation counts: zero enabled scheduler approvals, zero recorded job runs, no `cron.job`, zero active non-synthetic owners, zero externally active outbox entries. These are observed facts, not proof of recovery, SMTP, backup restoration or provider delivery.

## Completed local work

1. **Complete reporting** — `public.business_report` returns authorized aggregate JSON from one statement snapshot. Date boundaries use Dubai time; branch and sport filters use current permissions. Coach data uses current assignment/substitution checks, not the capped 200-session UI projection. Canceled sessions, canceled/unmarked roster entries, publication dates and reversed progressions are handled explicitly. Finance requires its own permission; both Business Reports and management collections use this endpoint. Detail pagination never supplies these totals. Snapshot time and refresh are visible. Family payments may cover several sports, so finance is explicitly all-sport for the selected branch. No invented per-sport allocation or historical time-travel balance is claimed.
2. **Registration handoff** — Staff can issue/copy/revoke a 48-hour lead-bound link. Only a 256-bit capability digest is stored; the token is in the browser fragment rather than an HTTP query. The parent signs in/creates their family in another tab, returns and explicitly selects an already-owned child. Continuation preserves lead/enquiry IDs and is idempotent. It cannot grant guardianship, merge by email/phone, overwrite another child's link or silently replace a booked unlinked trial. No WhatsApp message is sent.
3. **Development automation** — Versioned synthetic policies extend the existing durable job runner, retries, history and approval controls. Monthly jobs create factual drafts from published assessments, with a visible 30-evidence limit. Assigned coaches submit; existing independent review/publication gates and PDF paths remain. Only publication notifies the parent. Completion certificates follow approved unreversed progression, use existing private PDF/access/history and are deduplicated; reversal withdraws access. No automatic reissue overrides a manual revocation. Policies and clocks are not enabled by migrations.
4. **Coach communication** — Child/enrollment-scoped conversations, current coach/substitute/branch authority, explicit operations oversight, reviewed coach drafts, immutable submitted body, current-parent access, per-displayed-message read receipts and escalation. Parents cannot see draft replies; former coaches lose history immediately when authorization changes. No external-delivery badge is fabricated. Policies are disabled until approved; synthetic review policies were enabled in rolled-back SQL tests and a real local MFA/HTTP rehearsal, then disabled; the temporary verification owner was deactivated. Attachments and automatic retention deletion remain absent.
5. **Provider interfaces** — Vendor-neutral Payment/WhatsApp contracts and inactive implementations. Checkout, reconciliation, webhook verification and template delivery throw `provider_not_configured`; no fake success adapter, ledger posting or outbox delivery is possible. These are interface boundaries, not a completed vendor sandbox integration. Signed vendor events, reconciliation/refund posting and approved recipient delivery must be implemented and verified against the selected vendors before activation.
6. **Synthetic migration executor** — Extends the existing bounded planner with values materialized from the same validated file reads. Private staged source rows, hashes, reviewed mapping reference, deterministic identities, checkpoints, exact replay, counts/money/session reconciliation and own-batch reversal are implemented. Reversal checks current row snapshots, explicit downstream activity and database dependencies atomically. History/manifests survive reversal. Families/children are created without guardians; the planner's membership, opening-balance and payment-history snapshot intents remain separate. No membership acceptance, live entitlement activation or fresh collections are fabricated. `scripts/migration/execute-local.ts` can target only the named local container with explicit synthetic acknowledgement. Operational cutover adapters and real export compatibility remain unfinished until the source and rules are approved.
7. **Targeted quality fixes** — English/Arabic labels, removal of contradictory pagination notice from complete reports, error/permission states, retained local-only authenticator exemption and authenticator-required policy activation. Public resize/reduced-motion reading-position restoration waits for settled geometry and updates Lenis limits; regression tests remain unchanged.

## Verification and evidence

See `evidence/completion-summary.json` and the redacted evidence archive for the final build/test hashes and results. SQL tests run in a fresh disposable database, use declared synthetic records and rollback fixtures. They do not fake a hosted owner's authentication or prove provider activation. HTTP checks use separate real local sessions for Parent, Sales, Front Desk, Coach, Head Office and Owner, including logout/login. The local owner's explicitly requested demo-only MFA exception is recorded separately; source replay still checks normal MFA denial.

The original 27-section matrix is retained in `MEETING-ACCEPTANCE.md`. Later expanded requests are tracked separately in `EXPANDED-SCOPE-REGISTER.md`. This report is not a signed support agreement or production acceptance.

Local database advisors retain one RLS initialization warning and informational index/private-table findings; the database linter retains eleven unused-variable warnings in earlier functions. Both commands exit successfully. Full redacted findings accompany the evidence; this is not a zero-warning claim. The repeated HTTP/browser runs reached the local login throttle; only the exact isolated local test bucket was cleared before the final sequential browser run. Hosted limits were unchanged.

## Remaining work and activation gates

- Restore connector access to the correct private Site, match its source/version/configuration, push exact reviewed source, deploy, verify `/api/release`, apply only genuinely missing mapped migrations with backup/rollback evidence, and repeat the complete hosted journey. Browser viewer access alone cannot publish a version.
- Approve actual sport/level, absence, renewal, certificate/report and coach oversight/retention policies. Real policies remain inactive; no unapproved defaults were activated.
- Receive authorized Mindbody exports/mappings and cutover decisions. Implement approved operational conversion of retained membership/opening snapshots, validate exact export coverage and rehearse in an isolated restore before real import. Synthetic snapshot execution is not a claim of completed migration.
- Select gateway/WhatsApp vendors and provide sandbox/test-recipient approval. Vendor-specific verification, reconciliation/refund handling and delivery are intentionally not simulated.
- Establish real owner MFA/recovery, named operators, SMTP allowlists, scheduler approval, monitoring and an isolated backup restoration/rollback rehearsal. Those hosted acceptance gates remain unverified.

## Compact remaining-decision register

| Decision | Responsible party | Current behavior | Required acceptance evidence |
|---|---|---|---|
| Branches, venues, sports, levels and placement | KAFOU operations/technical lead | Synthetic/provisional configuration | Approved bilingual timetable, age/level criteria and placement examples |
| Absence, make-up, expiry and cancellation | KAFOU operations | Synthetic rules only | Approved rules and edge-case examples including cross-branch eligibility |
| Renewal, report/certificate triggers | KAFOU operations/technical lead | Real policies and scheduler inactive | Signed-off triggers, review roles, deduction/expiry rules and hosted dedupe checks |
| Coach oversight and communication/media retention | KAFOU operations/privacy owner | Policy inactive; attachments and automatic deletion absent | Oversight/retention decision, deletion scope if approved, revoked-access checks |
| Mindbody export, mapping and operational cutover | KAFOU data owner and migration operator | Synthetic snapshots only | Authorized export, approved entitlement/opening-ledger conversions, guardian claims, reconciliation and reversal rehearsal |
| Real owner, MFA/recovery and named operators | KAFOU owner | Synthetic local access; hosted real-owner recovery unverified | Chosen verified identity, genuine MFA, revocation and recovery evidence |
| SMTP and approved recipients | KAFOU communications owner | No real delivery enabled | Approved sender/recipient allowlist and actual confirmation/recovery delivery |
| Scheduler, monitoring and escalation | Release operator and KAFOU support owner | No hosted clock, approvals or runs observed | Approved activation, failed-run alerts, pause/retry/dedupe and escalation rehearsal |
| Backups, files and rollback | Release/recovery operator | Local database restore only | Authorized hosted backup, isolated full restore including protected files, previous-artifact compatibility and recovery objectives |
| Payment and WhatsApp vendors | KAFOU commercial/communications owner | Explicitly inactive; no simulated success | Vendor selection and separately authorized sandbox implementation/testing |
| Publishing access | Site owner | Read-only check returns project not found; local-only instruction now applies | Restore correct owning workspace access when hosted work is requested again |
